func foo(a: int, b: int) -> int {
    return a * b;
}

func main() -> int {
    let g: int = foo(1, 2);
    let a: int;
    a = foo(10, 9);

    let arr = [1,3,4,5,114514];
    for (let i=0; i<a; i++) {
        g += i*2;
    }
    return g + arr[4] - arr[0];
}
