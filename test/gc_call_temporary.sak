func make_word(text: string) -> string {
    return concat_string(text, "!");
}

func main() -> i32 {
    let value = "";

    repeat(20000) {
        value = concat_string(make_word("A"), make_word("B"));
    }

    __println(value);
    return 0;
}
