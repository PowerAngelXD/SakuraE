func keep(text: string) -> string {
    let tmp = concat_string("x", "y");
    return text;
}

func main() -> i32 {
    let value = "";

    repeat(20000) {
        value = keep(concat_string("ke", "ep"));
    }

    __println(value);
    return 0;
}
